<view class="tab-bar">
  <view
    wx:for="{{tabs}}"
    wx:key="path"
    class="tab-bar__item {{current === item.key ? 'tab-bar__item--active' : ''}}"
    data-path="{{item.path}}"
    bindtap="onTap"
  >
    <text class="tab-bar__text">{{item.text}}</text>
  </view>
</view>
